export const pointRelativeToCanvas = (
  x: number,
  y: number,
  canvasRect: DOMRect | undefined
): { x: number; y: number } | undefined => {
  if (!canvasRect) return;
};

// export const drawOnCanvas = (ref: React)